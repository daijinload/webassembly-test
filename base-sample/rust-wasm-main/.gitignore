# 追加した場合、反映されないことがあるため、その場合は下記コマンドを実行する
# 色々と変更が出るが、git add --allと、git commit してあげれば、ファイルは消えずにきれいになる。
# git rm -r --cached .

# 拡張子なしのファイルをコミットしない指定（Goでバイナリ生成したものをコミットしない指定）
#*
!*/
!*.*
debug

/**/go/
/**/.vscode/
/**/pkg/
/**/.idea/

# vscode
microsoft.gpg
__debug_bin

# bin files
/**/*bin/

# Generated by Cargo
# will have compiled files and executables
/target/

# Remove Cargo.lock from gitignore if creating an executable, leave it for libraries
# More information here https://doc.rust-lang.org/cargo/guide/cargo-toml-vs-cargo-lock.html
Cargo.lock

# These are backup files generated by rustfmt
**/*.rs.bk

*.wasm
